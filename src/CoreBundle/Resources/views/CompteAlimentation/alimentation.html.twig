{% extends 'CoreBundle:User:base.html.twig' %}

{% block title %}Auto Alimetation - {{parent()}}{% endblock %}

{% block _container %}
<div class="row">
    <div class="page-header">
        <h3>Auto Alimetation</h3>
    </div>
    {% if as is not empty %}
    {% set total = 0 %}
    <div class="row">
        <div class="col-lg-12">
            <table class="table table-striped table-bordered table-hover">
                <tr>
                    <th>Date</th>
                    <th>Gain</th>
                </tr>
                {% for i in as %}
                {% set total = total + i.gain %}
                <tr>
                    <td>{{i.createAt|date('d M Y')}}</td>
                    <td>{{i.gain}}</td>
                </tr>
                {% endfor %}
                <tr>
                    <th>Total</th>
                    <td>{{total}}</td>
                </tr>
            </table>
        </div>
    </div>
    {% else %}
    Aucun gain sur ce compte.
    {% endif %}
</div>
<br/>
{% endblock %}